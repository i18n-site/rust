use aok::Result;

use crate::{errlog, recover};

pub async fn watch(result: Result<()>) {
  // xerr::log!(
  //   async move {
  //     if let Err(err) = result {
  //       dbg!(&err);
  //     }
  //   }
  //   .await
  // );
}
